@page "/PerfectLost"
@inject HomeDataService service

@if (gamelist != null) {
    <GameList Model="gamelist"></GameList>

}

@code {
    private GameListViewModel gamelist;

    protected override async Task OnInitializedAsync() {
        gamelist = await service.GetPerfectLostGameListModel();
    }
}
